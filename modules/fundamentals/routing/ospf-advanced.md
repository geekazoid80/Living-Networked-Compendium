---
id: RT-005
title: "OSPF Advanced — Multi-Area, Redistribution & Filtering"
description: "Multi-area OSPF design, LSA filtering, summarisation, virtual links, stub/NSSA areas, and route redistribution into and out of OSPF."
version: "1.0.0"
status: draft
human_reviewed: false
ai_assisted: "drafting"
module_type: concept
estimated_time: 60
prerequisites:
  - RT-004
  - IP-002
learning_path_tags:
  - DNE
difficulty: advanced
tags:
  - ospf
  - routing
  - multi-area
  - redistribution
  - lsa-filtering
  - stub-area
created: 2026-04-19
updated: 2026-04-19
---

# RT-005 — OSPF Advanced — Multi-Area, Redistribution & Filtering

## The Problem

A single OSPF area works for small networks. When the network grows to hundreds of routers, the Link State Database (LSDB) grows with it. Every router holds the full topology map. Every topology change triggers SPF recalculation on every router simultaneously. Convergence slows; memory consumption grows; CPU spikes.

### Step 1: Divide the topology into areas

Group routers into areas. Each area runs SPF only for its own topology. Routers at the boundary between areas (**ABRs — Area Border Routers**) summarise the internal topology and advertise summary routes into other areas, rather than flooding full topology detail everywhere. Area 0 (the backbone) is the transit area — all inter-area traffic flows through it.

### Step 2: Filter and summarise at the boundary

Not every route from one area needs to be seen in precise detail by another area. The ABR can summarise ten /24 prefixes into one /20 summary — smaller LSDB, less SPF work, controlled topology information flow. A distribution point within an area can filter which routes are advertised or received using distribute-lists and prefix-lists.

### Step 3: What about external routes?

OSPF runs inside one routing domain. But the network also uses EIGRP, static routes, and connected interfaces from other systems. These can be **redistributed** into OSPF as **Type 5 LSAs** (external routes visible everywhere), or Type 7 (in Not-So-Stubby Areas). The redistribution boundary is the **ASBR (Autonomous System Boundary Router)**.

### What You Just Built

| Scenario element | Technical term |
|---|---|
| Router at the boundary of two areas | ABR (Area Border Router) |
| Router redistributing external routes | ASBR (AS Boundary Router) |
| Summarised inter-area route | Type 3 LSA (Summary LSA) |
| External route injected into OSPF | Type 5 LSA (External LSA) |
| Area that blocks external LSAs | Stub area |
| Area that allows limited external routes | NSSA (Not-So-Stubby Area) |

---

## Learning Objectives

After completing this module you will be able to:

1. Explain why multi-area OSPF is necessary at scale and how the area hierarchy works.
2. Describe the role of ABR and ASBR and the LSA types they generate.
3. Configure inter-area summarisation on an ABR.
4. Explain stub areas and NSSA and when to use each.
5. Configure route redistribution into OSPF with seed metrics.
6. Use distribute-lists and prefix-lists to filter OSPF advertisements.
7. Explain virtual links and when they are needed.

---

## Prerequisites

- RT-004 — OSPF Fundamentals (LSA types 1–5, neighbor state machine, areas, cost)
- IP-002 — Subnetting (understanding prefix ranges for summarisation)

---

## Core Content

### Multi-Area OSPF Review

Every OSPF router in an area maintains an identical LSDB for that area. SPF is run once per area per topology change. The area count controls SPF scope.

**Area 0 (backbone):** All other areas must connect to it. Inter-area traffic flows through Area 0. ABRs connect non-backbone areas to Area 0 — they maintain separate LSDBs for each connected area.

**Hierarchy rule:** Non-backbone areas cannot connect to each other directly for inter-area routing. All inter-area routes must transit Area 0. If a non-backbone area cannot physically reach Area 0, a **virtual link** is used as a workaround.

### LSA Types in Multi-Area OSPF

| LSA Type | Name | Generated by | Scope | Contains |
|---|---|---|---|---|
| 1 | Router LSA | Every router | Within area | Router's interfaces and costs |
| 2 | Network LSA | DR on broadcast | Within area | Multi-access segment topology |
| 3 | Summary LSA | ABR | Into connected areas | Inter-area prefix reachability |
| 4 | ASBR Summary LSA | ABR | Into areas | ASBR's location (for reaching Type 5) |
| 5 | External LSA | ASBR | All areas (except stub) | External prefix + metric |
| 7 | NSSA External LSA | ASBR in NSSA | Within NSSA only | External prefix in NSSA (converted to Type 5 at ABR) |

### Inter-Area Summarisation

ABRs summarise Type 1/2 LSAs from one area into Type 3 Summary LSAs advertised to other areas. Configuration on the ABR:

```
area <area-id> range <prefix> <mask>
```

This replaces multiple specific Type 3 LSAs with a single summary route. The summary cost is the maximum cost among all summarised prefixes (not the sum).

**Benefits:**
- Smaller LSDB in other areas.
- Topology change in one area (flapping link) doesn't trigger SPF in all areas — ABR only updates the summary if the summary itself changes (if any component is reachable).
- Smaller routing tables in non-ABR routers.

**Discard route:** When summarisation is configured, the ABR installs a null0 (discard) route for the summary prefix in its own routing table. This prevents routing loops if the specific routes are absent — packets matching the summary with no more-specific route are dropped (rather than forwarded based on a default route toward a loop).

### Stub Areas

A **stub area** does not receive Type 5 (External) LSAs. The ABR injects a default route (Type 3) instead. Routers in stub areas send unknown-destination traffic toward the ABR, which handles it.

| Area type | Receives Type 3 | Receives Type 5/7 | ABR injects default? |
|---|---|---|---|
| Normal | Yes | Yes | No |
| Stub | Yes | No | Yes |
| Totally Stubby | Only default | No | Yes |
| NSSA | Yes | No (Type 7 internally) | Optional |
| Totally NSSA | Only default | No (Type 7 internally) | Yes |

**Totally stubby (Cisco extension):** ABR doesn't send Type 3 LSAs either — only a default route. Routers have the smallest possible LSDB. Use in hub-spoke branch topologies where all traffic exits via the ABR.

**NSSA:** A stub area that also has an ASBR (e.g., a branch site importing static routes from a CPE). The ASBR generates Type 7 LSAs locally. The ABR converts them to Type 5 when advertising into the backbone. Type 5 from the rest of the network is still blocked.

### External Route Redistribution

The ASBR imports routes from other sources into OSPF using `redistribute`. Each redistributed route appears as a Type 5 LSA.

**External metric types:**

- **E1 (Type 1):** The metric at any router = the OSPF cost to reach the ASBR + the external metric. Prefers the closest ASBR. Used when multiple ASBRs inject the same external prefix.
- **E2 (Type 2):** The metric everywhere = the external metric only. OSPF internal cost to reach the ASBR is ignored. Default in IOS. Simpler but doesn't prefer closer ASBR.

**Seed metric:** When redistributing into OSPF, a seed metric must be set (OSPF can't accept metric 0). Default: `metric 20` for most sources on Cisco. Best practice: set explicitly.

```
redistribute static subnets metric 10 metric-type 1
redistribute eigrp 100 subnets metric 20 metric-type 1 route-map FILTER-IN
```

The `subnets` keyword is required on Cisco IOS to redistribute non-classful (non-class-boundary) routes.

### Route Filtering in OSPF

**Distribute-list:** Filters routes from being installed in the routing table (not from being advertised in LSAs). Affects only the local router's RIB.

```
distribute-list prefix DENY-PREFIX in
```

**Prefix-list as ABR filter:** On the ABR, use `area range not-advertise` to prevent a specific prefix from being advertised as a Type 3 LSA into another area.

**Route-map on redistribution:** Use a route-map with match/set clauses to selectively import external routes and control their attributes (metric, tag, metric-type).

??? supplementary "OSPF Database Filtering with filter-list"
    On Cisco IOS, `area <id> filter-list prefix <name> in|out` filters which Type 3 LSAs the ABR advertises into or from an area. This is more precise than distribute-list because it prevents LSA propagation (not just RIB installation). Use `filter-list out` on an ABR to prevent specific prefixes from being summarised or advertised into the backbone.

### Virtual Links

A virtual link is a logical OSPF adjacency that extends Area 0 through a transit area. Required when:
- A new area cannot physically connect to Area 0 (the "partitioned backbone" problem).
- An area is temporarily cut off from Area 0 due to a physical failure.

Virtual links are a workaround, not a design recommendation. They add complexity, and adjacency through a transit area means the virtual link goes down if the transit area loses connectivity to both ends. Prefer physical design changes (add a direct Area 0 link) over relying on virtual links.

Configuration (on both ABRs bounding the transit area):
```
area <transit-area-id> virtual-link <remote-ABR-router-id>
```

---

## Vendor Implementations

=== "Cisco IOS-XE"

    ```
    ! Multi-area OSPF
    router ospf 1
     router-id 10.0.0.1

    ! ABR summarisation (Area 1 → Area 0)
     area 1 range 10.1.0.0 255.255.0.0

    ! Stub area
     area 2 stub

    ! Totally stubby (Cisco only — on ABR only)
     area 2 stub no-summary

    ! NSSA
     area 3 nssa

    ! Redistribute static routes into OSPF
     redistribute static subnets metric 20 metric-type 1

    ! Route filtering (distribute-list)
     distribute-list prefix DENY-SOME in

    ! Prefix list for filtering
    ip prefix-list DENY-SOME seq 5 deny 192.168.100.0/24
    ip prefix-list DENY-SOME seq 10 permit 0.0.0.0/0 le 32

    ! Verification
    show ip ospf database
    show ip ospf border-routers
    show ip ospf summary-address
    ```

    Full configuration reference: [https://www.cisco.com/c/en/us/td/docs/ios-xml/ios/iproute_ospf/configuration/xe-17/iro-ospf-xe-17-book.html](https://www.cisco.com/c/en/us/td/docs/ios-xml/ios/iproute_ospf/configuration/xe-17/iro-ospf-xe-17-book.html)

=== "Juniper (Junos)"

    ```
    set protocols ospf area 0.0.0.1 area-range 10.1.0.0/16
    set protocols ospf area 0.0.0.2 stub default-metric 10
    set protocols ospf area 0.0.0.3 nssa

    # Redistribute static routes
    set policy-options policy-statement REDIST-STATIC term STATIC from protocol static
    set policy-options policy-statement REDIST-STATIC term STATIC then accept
    set protocols ospf export REDIST-STATIC

    # Verification
    show ospf database
    show ospf route
    ```

    Full configuration reference: [https://www.juniper.net/documentation/us/en/software/junos/ospf/topics/topic-map/ospf-multi-area.html](https://www.juniper.net/documentation/us/en/software/junos/ospf/topics/topic-map/ospf-multi-area.html)

=== "Nokia SR-OS"

    ```
    configure router ospf area 1
        area-range 10.1.0.0/16

    configure router ospf area 2
        stub
        default-metric 10

    configure router ospf
        export "REDIST-STATIC"

    configure router policy-options
        policy-statement "REDIST-STATIC"
        entry 10
            from protocol static
            action accept
        exit
    ```

    Full configuration reference: [https://documentation.nokia.com/sr/23-3/titles/ospf.html](https://documentation.nokia.com/sr/23-3/titles/ospf.html)

=== "Arista EOS"

    ```
    router ospf 1
       area 1 range 10.1.0.0/16
       area 2 stub
       area 3 nssa
       redistribute static metric 20 metric-type 1
       distribute-list prefix DENY-SOME in

    ip prefix-list DENY-SOME seq 5 deny 192.168.100.0/24
    ip prefix-list DENY-SOME seq 10 permit 0.0.0.0/0 le 32
    ```

    Full configuration reference: [https://www.arista.com/en/um-eos/eos-ospf](https://www.arista.com/en/um-eos/eos-ospf)

---

## Common Pitfalls

1. **Forgetting `subnets` on Cisco redistribution.** Without `redistribute static subnets`, only classful routes are redistributed. All /25–/30 and other non-classful routes are silently omitted. Always include `subnets`.

2. **Missing seed metric.** OSPF requires a non-zero metric for redistributed routes. On some platforms, redistribution without explicit metric fails or uses an unpredictable default. Always set explicitly: `metric <n>`.

3. **E2 metric type when multiple ASBRs import the same prefix.** With E2 (default), all routers in the domain see the same external metric regardless of which ASBR is closer. Traffic may not take the optimal path to the nearest ASBR. Use E1 when multiple ASBRs import the same external prefix.

4. **Virtual link through a stub area.** Virtual links cannot traverse stub or NSSA areas — stub areas block certain LSA types that virtual links need. The transit area for a virtual link must be a normal OSPF area.

5. **ABR summarisation discard route.** When `area range` is configured, the ABR installs a null0 route for the summary. If a component route is withdrawn (e.g., a specific /24 goes down), the summary persists (other components still exist), but packets for the withdrawn /24 are blackholed at the ABR. This is correct behaviour — the ABR correctly aggregates and drops traffic for unreachable sub-prefixes within the summary range.

---

## Practice Problems

**Q1.** Area 1 has 50 routers with prefixes in the 10.1.0.0/16 range. Without summarisation, how many Type 3 LSAs does Area 2 receive for Area 1 routes? With summarisation using `area 1 range 10.1.0.0/16`?

??? answer
    Without summarisation: up to 50 Type 3 LSAs (one per prefix from Area 1). With summarisation: 1 Type 3 LSA (the 10.1.0.0/16 summary). The ABR generates one summary LSA instead of propagating individual Type 1/2 details.

**Q2.** An area is configured as stub. What types of LSAs does it receive? What does it NOT receive?

??? answer
    A stub area receives Type 1 (router), Type 2 (network), and Type 3 (summary inter-area) LSAs. It does NOT receive Type 5 (external) LSAs. The ABR injects a default route (Type 3, 0.0.0.0/0) so routers can reach external destinations via the ABR.

**Q3.** You redistribute EIGRP into OSPF with metric-type 2 (E2). Two ASBRs (A1 and A2) both redistribute the same prefix 192.168.50.0/24. From a router deep in the network, which ASBR is preferred?

??? answer
    With E2, the metric is the external seed metric only — OSPF internal cost to reach the ASBR is not added. Both ASBRs would have the same external metric (E2 = seed metric only). The router uses the lowest cost to reach the ASBR router ID as tiebreaker — effectively choosing the topologically closer ASBR. But if both have the same internal cost to reach, it becomes a tie broken by router ID. Using E1 would explicitly prefer the closer ASBR.

---

## Summary & Key Takeaways

- Multi-area OSPF limits SPF scope — each area runs SPF independently; ABRs summarise between areas.
- **Area 0** is the backbone; all inter-area routes transit it.
- **ABRs** generate Type 3 LSAs; **ASBRs** generate Type 5 LSAs.
- Inter-area summarisation (`area range`) reduces LSDB size and prevents micro-failures from rippling across areas.
- **Stub areas** block Type 5 LSAs; the ABR injects a default route.
- **Totally stubby** (Cisco) also blocks Type 3 LSAs — only a default route enters.
- **NSSA** allows a local ASBR but blocks inbound Type 5; Type 7 LSAs are converted to Type 5 at the ABR.
- Redistribution requires explicit seed metrics and `subnets` keyword (Cisco).
- E1 metric type prefers the closer ASBR; E2 ignores internal cost to the ASBR.
- Virtual links extend Area 0 through transit areas — use as a last resort.

---

## Where to Next

- **RT-007 — BGP Fundamentals:** BGP and OSPF redistribution is common at ISP boundaries.
- **RT-009 — Route Redistribution & Policy:** Mutual redistribution between OSPF and other protocols.
- **CT-001 — MPLS Fundamentals:** OSPF is typically the IGP underpinning MPLS label distribution.

---

## Standards & Certifications

| Standard / Cert | Relevance |
|---|---|
| RFC 2328 | OSPF Version 2 (IPv4) |
| RFC 5340 | OSPFv3 for IPv6 |
| RFC 3101 | OSPF NSSA Option |
| Cisco CCNP Enterprise | Multi-area OSPF, summarisation, stub/NSSA, redistribution |
| Juniper JNCIP | OSPF advanced configuration |

---

## References

- RFC 2328 — OSPF Version 2. [https://www.rfc-editor.org/rfc/rfc2328](https://www.rfc-editor.org/rfc/rfc2328)
- RFC 5340 — OSPF for IPv6. [https://www.rfc-editor.org/rfc/rfc5340](https://www.rfc-editor.org/rfc/rfc5340)
- RFC 3101 — The OSPF Not-So-Stubby Area (NSSA) Option. [https://www.rfc-editor.org/rfc/rfc3101](https://www.rfc-editor.org/rfc/rfc3101)

---

## Attribution & Licensing

- Module content: original draft, AI-assisted (Claude Sonnet 4.6), 2026-04-19.
- No third-party text reproduced.
- License: [CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/)

<!-- XREF-START -->
## Cross-References

### Vendor Feature Mapping

| Feature | Cisco IOS-XE | Juniper (Junos) | Nokia SR-OS | Arista EOS |
|---|---|---|---|---|
| Area summarisation | `area <id> range <pfx> <mask>` | `area-range <pfx/len>` | `area-range <pfx/len>` | `area <id> range <pfx/len>` |
| Stub area | `area <id> stub` | `stub` under area | `stub` | `area <id> stub` |
| Totally stubby | `area <id> stub no-summary` | `stub no-summaries` | `no-summaries` | `area <id> stub` + no summary |
| NSSA | `area <id> nssa` | `nssa` | `nssa` | `area <id> nssa` |
| Redistribute static | `redistribute static subnets` | `export` policy | `export` policy | `redistribute static` |
| Metric type | `metric-type 1\|2` | `external <type>` | `metric-type 1\|2` | `metric-type 1\|2` |

### Modules That Reference This Module

| Module ID | Title | Relationship |
|---|---|---|
| RT-009 | Route Redistribution & Policy | Redistribution between OSPF and other protocols |
| CT-001 | MPLS Fundamentals | OSPF is the typical IGP for MPLS underlay |

### Modules This Module References

| Module ID | Title | Relationship |
|---|---|---|
| RT-004 | OSPF Fundamentals | Prerequisite — single-area OSPF, LSA types 1–5, DR/BDR |
| IP-002 | IP Subnetting | Prefix ranges for summarisation |
<!-- XREF-END -->
